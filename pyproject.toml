[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "poroelasticity-pi-deeponet"
version = "0.1.0"
description = "Physics-informed MIONet and Gauss-Seidel solvers for one-dimensional Biot consolidation."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.26,<2.0",
  "pandas>=2.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
]
neural = [
  "tensorflow>=2.16,<2.22",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
